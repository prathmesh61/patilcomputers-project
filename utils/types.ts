import { LucideIcon } from "lucide-react"

export type TService = {
  title: string
  Icon: LucideIcon
  description: string
}
export type TLinks = {
  name: string
  link: string
  Icon?: LucideIcon
}
